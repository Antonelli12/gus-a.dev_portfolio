import { Bitcount_Grid_Single, Inter, Sixtyfour } from "next/font/google";

export const headingFont = Bitcount_Grid_Single({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});

export const highlightFont = Sixtyfour({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});

export const bodyFont = Inter({
  subsets: ["latin"],
  display: "swap",
});